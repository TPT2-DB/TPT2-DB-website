import React from 'react'

export default function Header() {
	const { data: session } = useSession()
	
	return (
		<div className="header w-screen h-auto bg-blue-500 drop-shadow-sm fixed flex">
			
		</div>
	)
}
